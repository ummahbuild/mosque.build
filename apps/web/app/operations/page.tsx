import areas from "@/data/product-areas.json";import {EmergencyReadinessWorkspace} from "@/components/emergency-readiness-workspace";import {OperationsWorkspace} from "@/components/operations-workspace";import {ProductAreaPage} from "@/components/product-area-page";import {WorkOrderWorkspace} from "@/components/work-order-workspace";export const metadata={title:"Opening and operations workspace",description:"Build a device-local handover asset register and accountable maintenance rhythm."};export default function Page(){const area=areas.find(a=>a.slug==="operations")!;return <ProductAreaPage area={area} insert={<><OperationsWorkspace/><WorkOrderWorkspace/><EmergencyReadinessWorkspace/></>}/>}
