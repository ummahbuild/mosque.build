import {SiteFooter} from "@/components/site-footer";
import {SiteHeader} from "@/components/site-header";
import {SourcePartnerGallery} from "@/components/source-partner-gallery";
import {partnerBriefs,sourceRecords} from "@/lib/source-gallery";

export const metadata={title:"Sources + Partner Discovery",description:"A provenance-aware gallery of recorded sources and unverified partner discovery briefs from the mosque.build repository."};

export default function Page(){return <><SiteHeader/><main><section className="areaHero sourceHero"><div><div className="eyebrow gold">SOURCE + PARTNER DISCOVERY GALLERY</div><h1>See what is sourced, queued and still unverified.</h1><p className="heroLead">Named references remain separate from regional partner research briefs. A discovery target is never presented as a verified vendor, partner, price or endorsement.</p></div></section><section className="section"><div className="batchPrinciples"><article><b>{sourceRecords.length}</b><span>named source records</span></article><article><b>{partnerBriefs.length}</b><span>partner discovery briefs</span></article><article><b>{new Set(partnerBriefs.map(item=>item.region)).size}</b><span>regions represented in queues</span></article><article><b>{new Set(partnerBriefs.map(item=>item.category)).size}</b><span>goods + service categories</span></article></div><div className="methodBox batchNotice"><b>Evidence boundary</b><p>Statuses shown here are recorded repository metadata and may not prove current validity. Before production use, verify the publisher, URL, date, jurisdiction, rights, applicability and review state. Partner briefs contain no verified organizations yet.</p></div><SourcePartnerGallery sources={sourceRecords} partners={partnerBriefs}/></section></main><SiteFooter/></>}
