import Link from "next/link";
import {SiteFooter} from "@/components/site-footer";
import {SiteHeader} from "@/components/site-header";
import areas from "@/data/product-areas.json";

const connections=["One project brief that grows with the mosque","Community decisions and responsibilities kept visible","Qibla, prayer capacity and room planning","Accessible journeys from arrival to prayer and wudu","Sources and dates beside external information","Qualified review at the right moments","Cost and maintenance considered with design","Responsive tools for committees and site teams"];
export default function Page(){return <><SiteHeader/><main><section className="areaHero featureHero"><div><div className="eyebrow gold">HOW MOSQUE.BUILD WORKS</div><h1>One journey from first conversation to long-term care.</h1><p className="heroLead">Begin with a community brief, then open focused workspaces as the site, team, funding and mosque become clearer.</p><div className="heroActions"><Link href="/start" className="button">Start a project ↗</Link><Link href="/resources" className="secondaryButton lightButton">Find the next useful step</Link></div></div></section><section className="section"><div className="sectionHeading"><div><div className="eyebrow dark">CHOOSE WHERE TO WORK</div><h2>Move into the part of the journey you need today.</h2></div><Link className="secondaryButton" href="/prototypes">See mosque project concepts →</Link></div><div className="productAreaGrid featureAreaGrid">{areas.map(area=><Link href={`/${area.slug}`} key={area.slug}><span>{area.eyebrow}</span><h2>{area.title}</h2><p>{area.summary}</p><b>Open workspace →</b></Link>)}</div></section><section className="darkSection"><div className="section"><div className="eyebrow gold">KEEP DECISIONS CONNECTED</div><h2>Important questions follow the project.</h2><div className="crossGrid">{connections.map((item,index)=><article key={item}><span>{String(index+1).padStart(2,"0")}</span><b>{item}</b></article>)}</div></div></section></main><SiteFooter/></>}
