import Link from "next/link";
import {SiteFooter} from "@/components/site-footer";
import {SiteHeader} from "@/components/site-header";
import areas from "@/data/product-areas.json";

const cross=["Stable project identity + version history","Community requirements + decision rights","Qibla + prayer-capacity intelligence","Accessibility across design + operations","Source/provenance + confidence states","Professional + authority review gates","Lifecycle cost + maintenance implications","Multilingual and low-bandwidth workflows"];

export default function Page(){return <><SiteHeader/><main><section className="areaHero featureHero"><div><div className="eyebrow gold">COMPLETE PRODUCT MAP</div><h1>One mosque project. Every stage connected.</h1><p className="heroLead">mosque.build is designed around the lifecycle of a real mosque—not around isolated software categories.</p><div className="heroActions"><Link href="/start" className="button">Start a project ↗</Link><Link href="/features/priority-800" className="secondaryButton lightButton">Explore priority 800</Link></div></div></section><section className="section"><div className="sectionHeading"><div><div className="eyebrow dark">PRODUCT AREAS</div><h2>Move from public context into focused workflows.</h2></div><Link className="secondaryButton" href="/features/priority-800">Search records 701–800 →</Link></div><div className="productAreaGrid featureAreaGrid">{areas.map(area=><Link href={`/${area.slug}`} key={area.slug}><span>{area.eyebrow}</span><h2>{area.title}</h2><p>{area.summary}</p><b>{area.features.length} core workflow groups →</b></Link>)}</div></section><section className="darkSection"><div className="section"><div className="eyebrow gold">MOSQUE-SPECIFIC CROSS-CUTTING CAPABILITIES</div><h2>Features that connect across the lifecycle.</h2><div className="crossGrid">{cross.map((item,index)=><article key={item}><span>{String(index+1).padStart(2,"0")}</span><b>{item}</b></article>)}</div></div></section></main><SiteFooter/></>}
