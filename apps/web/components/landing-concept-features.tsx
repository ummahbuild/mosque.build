import Link from"next/link";import frames from"@/data/frame-registry.json";import{ConceptFeaturePreview}from"./concept-feature-preview";import type{ConceptFrame}from"@/lib/concept-features";
const wanted=["mosque-interior-design-studio-mockup","mosque-parking-compliance-dashboard","mosquebuild-waqf-library-dashboard-mockup","mosque-construction-progress-dashboard"];
export function LandingConceptFeatures(){const selected=wanted.map(slug=>(frames as ConceptFrame[]).find(frame=>frame.slug===slug)).filter((frame):frame is ConceptFrame=>!!frame);return <section className="section"><div className="sectionHeading"><div><div className="eyebrow dark">WORKING FEATURE PREVIEWS</div><h2>Move from a visual idea into the actual tool.</h2><p>Explore the capabilities represented in each frame, save useful ones to your project and continue in the connected workspace.</p></div><Link href="/prototypes" className="secondaryButton">Explore all {frames.length} feature studies</Link></div><div className="previewGrid">{selected.map(frame=><article className="previewCard" key={frame.id}><div className="previewCopy"><span className="miniTag">IMPLEMENTED FEATURE STUDY</span><h3>{frame.title}</h3></div><ConceptFeaturePreview frame={frame}/></article>)}</div></section>}
