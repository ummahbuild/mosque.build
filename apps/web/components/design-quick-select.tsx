"use client";
import Link from "next/link";import {useEffect,useState} from "react";import patterns from "@/data/pattern-library.json";
const key="mosque-build.pattern-shortlist.v1";
export function DesignQuickSelect(){const [saved,setSaved]=useState<string[]>([]);useEffect(()=>{try{setSaved(JSON.parse(localStorage.getItem(key)||"[]"))}catch{}},[]);const toggle=(id:string)=>setSaved(current=>{const next=current.includes(id)?current.filter(item=>item!==id):[...current,id];try{localStorage.setItem(key,JSON.stringify(next))}catch{}return next});return <section className="quickSelect"><div className="sectionHeading"><div><div className="eyebrow dark">QUICK SELECT</div><h2>Build a design-study shortlist now.</h2><p>Select pattern families for discussion, then review their historical context, application limits and sources.</p></div><Link className="secondaryButton" href="/patterns">Open full pattern explorer</Link></div><div className="quickSelectRail">{patterns.slice(0,6).map(item=><article key={item.id}><div className={`patternVisual pattern-${item.style}`} aria-hidden="true"/><small>{item.category}</small><h3>{item.title}</h3><button type="button" aria-pressed={saved.includes(item.id)} onClick={()=>toggle(item.id)}>{saved.includes(item.id)?"Selected ✓":"Add to design studies"}</button></article>)}</div><p className="quickSelectStatus" role="status" aria-live="polite">{saved.length} design {saved.length===1?"study":"studies"} saved on this device. <Link href="/my-project">Continue in my project →</Link></p></section>}
