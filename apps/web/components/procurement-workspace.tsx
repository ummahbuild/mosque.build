"use client";
import {useEffect,useMemo,useState} from "react";import items from "@/data/procurement-items.json";
const key="mosque-build.procurement.v1";
export function ProcurementWorkspace(){const [query,setQuery]=useState("");const [category,setCategory]=useState("All");const [compare,setCompare]=useState<string[]>([]);const [pack,setPack]=useState<string[]>([]);const [region,setRegion]=useState("");const [quantity,setQuantity]=useState("");const [notes,setNotes]=useState("");const [message,setMessage]=useState("");useEffect(()=>{try{const saved=JSON.parse(localStorage.getItem(key)||"{}");setCompare(saved.compare||[]);setPack(saved.pack||[]);setRegion(saved.region||"");setQuantity(saved.quantity||"");setNotes(saved.notes||"")}catch{}},[]);const persist=(next:{compare?:string[];pack?:string[];region?:string;quantity?:string;notes?:string})=>{const value={compare,pack,region,quantity,notes,...next};try{localStorage.setItem(key,JSON.stringify(value))}catch{}};const categories=["All",...new Set(items.map(item=>item.category))];const visible=useMemo(()=>items.filter(item=>(category==="All"||item.category===category)&&`${item.name} ${item.vendor} ${item.category} ${item.use}`.toLowerCase().includes(query.toLowerCase())),[query,category]);const compared=compare.map(id=>items.find(item=>item.id===id)).filter(Boolean) as typeof items;const toggleCompare=(id:string)=>{const next=compare.includes(id)?compare.filter(item=>item!==id):compare.length<3?[...compare,id]:compare;setCompare(next);persist({compare:next});if(compare.length>=3&&!compare.includes(id))setMessage("Compare up to three records at a time.")};const togglePack=(id:string)=>{const next=pack.includes(id)?pack.filter(item=>item!==id):[...pack,id];setPack(next);persist({pack:next})};const exportBrief=()=>{const payload={product:"mosque.build",kind:"procurement-scope-brief",exportedAt:new Date().toISOString(),notice:"Planning brief only. No quote, order, approval or supplier commitment.",deliveryRegion:region||"Not specified",quantityOrScope:quantity||"Not specified",requirements:notes||"Not specified",records:pack.map(id=>items.find(item=>item.id===id)).filter(Boolean)};const url=URL.createObjectURL(new Blob([JSON.stringify(payload,null,2)],{type:"application/json"}));const link=document.createElement("a");link.href=url;link.download="mosque-build-procurement-brief.json";link.click();URL.revokeObjectURL(url);setMessage("Planning brief exported as JSON.")};return <section className="procurementWorkspace"><div className="sectionHeading"><div><div className="eyebrow dark">SOURCE + COMPARE</div><h2>Build a procurement brief before contacting suppliers.</h2><p>Five repository records are shown with their observed source state. Listings are not recommendations, availability, technical approval or current quotes.</p></div><div className="procurementCounts"><b>{compare.length}/3</b><span>compared</span><b>{pack.length}</b><span>in scope brief</span></div></div><div className="procurementTools"><label><span>Search records</span><input type="search" value={query} onChange={event=>setQuery(event.target.value)} placeholder="Search product, vendor or use"/></label><label><span>Category</span><select value={category} onChange={event=>setCategory(event.target.value)}>{categories.map(item=><option key={item}>{item}</option>)}</select></label></div><p role="status" aria-live="polite">{message||`${visible.length} source records shown.`}</p><div className="procurementGrid">{visible.map(item=><article key={item.id}><div><small>{item.category} · {item.status.replace("_"," ")}</small><h3>{item.name}</h3><p>{item.vendor}</p></div><p>{item.use}</p><ul>{item.technical.map(point=><li key={point}>{point}</li>)}</ul><dl><div><dt>Observed</dt><dd>{item.observedAt}</dd></div><div><dt>Price</dt><dd>{item.price?`${item.price.currency} ${item.price.amount} · ${item.price.scope}`:"No sourced price"}</dd></div></dl><a href={item.sourceUrl} target="_blank" rel="noopener noreferrer">Open observed source ↗</a><div className="procurementActions"><button type="button" aria-pressed={compare.includes(item.id)} onClick={()=>toggleCompare(item.id)}>{compare.includes(item.id)?"Comparing ✓":"Compare"}</button><button type="button" aria-pressed={pack.includes(item.id)} onClick={()=>togglePack(item.id)}>{pack.includes(item.id)?"In brief ✓":"Add to brief"}</button></div></article>)}</div>{compared.length?<section className="compareTray"><div className="sectionHeading"><div><small>COMPARISON</small><h3>Compare evidence, not marketing claims.</h3></div><button type="button" onClick={()=>{setCompare([]);persist({compare:[]})}}>Clear comparison</button></div><div className="compareTable" role="table" aria-label="Product comparison"><div role="row"><b role="columnheader">Field</b>{compared.map(item=><b role="columnheader" key={item.id}>{item.name}</b>)}</div>{[["Vendor",(item:typeof items[number])=>item.vendor],["Category",(item:typeof items[number])=>item.category],["Observed",(item:typeof items[number])=>item.observedAt],["Price evidence",(item:typeof items[number])=>item.price?`${item.price.currency} ${item.price.amount}`:"Not recorded"],["Verification",(item:typeof items[number])=>item.verification.replaceAll("_"," ")]].map(([label,get])=><div role="row" key={String(label)}><b role="rowheader">{String(label)}</b>{compared.map(item=><span role="cell" key={item.id}>{(get as (x:typeof items[number])=>string)(item)}</span>)}</div>)}</div></section>:null}<section className="scopeBrief"><div><small>RFQ / SCOPE PREPARATION</small><h3>Package the question before requesting a quote.</h3><p>This export stays on your device until you choose to share it.</p></div><div><label>Delivery geography<input value={region} onChange={event=>{setRegion(event.target.value);persist({region:event.target.value})}} placeholder="Country, city or site"/></label><label>Quantity or scope<input value={quantity} onChange={event=>{setQuantity(event.target.value);persist({quantity:event.target.value})}} placeholder="Describe quantity or measured scope"/></label><label>Requirements and exclusions<textarea value={notes} onChange={event=>{setNotes(event.target.value);persist({notes:event.target.value})}} placeholder="Performance, samples, documents, installation, exclusions…"/></label><button type="button" disabled={!pack.length} onClick={exportBrief}>Export scope brief ({pack.length})</button></div></section></section>}
