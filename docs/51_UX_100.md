# Product-wide UX pass — 100 implemented improvements

This ledger records the concrete, shipped outcomes of the September 2026 UX pass. It is implementation evidence, not forward-looking marketing copy.

## Workspace orientation

1. Added a universal workspace-tool navigator to every product-area workspace.
2. Automatically discovers tools from rendered workspace content, including late-mounted client tools.
3. Excludes the workspace introduction from the tool count.
4. Gives otherwise unaddressable tools stable anchor IDs.
5. Avoids duplicate generated IDs.
6. Shows the current tool name while scrolling.
7. Shows the current tool’s position in the full tool set.
8. Tracks the current tool with IntersectionObserver.
9. Adds a visual workspace scroll-progress indicator.
10. Adds a searchable tool directory.
11. Searches both tool names and section eyebrows.
12. Trims search input before matching.
13. Displays a useful empty-search message.
14. Reports the total available tool count in the empty state.
15. Adds one-click previous-tool navigation.
16. Adds one-click next-tool navigation.
17. Disables previous navigation at the first tool.
18. Disables next navigation at the last tool.
19. Adds a direct return-to-workspace-top action.
20. Marks the currently viewed directory item with aria-current.
21. Announces current-tool changes through an aria-live region.
22. Gives the navigator a descriptive landmark label.
23. Gives previous and next buttons explicit accessible names.
24. Closes the directory after choosing a tool.
25. Closes the directory with Escape.
26. Auto-focuses directory search when opened.
27. Respects reduced-motion preference during tool jumps.
28. Keeps the navigator sticky beneath the universal header.
29. Keeps the navigator sticky below mobile safe areas.
30. Uses a constrained, scrollable directory on short screens.

## Responsive navigation

31. Locks document scrolling while the mobile menu is open.
32. Restores the prior document scroll behavior when it closes.
33. Closes the mobile menu when Escape is pressed.
34. Returns focus to the menu button after Escape.
35. Closes the mobile menu after route changes.
36. Closes the mobile menu after resizing to desktop width.
37. Closes the mobile menu after an outside pointer action.
38. Traps keyboard focus inside the open mobile navigation.
39. Loops Shift+Tab from the first control to the last.
40. Loops Tab from the last control to the first.
41. Makes the closed mobile navigation inert.
42. Removes closed mobile links from the tab sequence.
43. Adds a visible “Explore mosque.build” mobile-menu heading.
44. Adds the contextual project action to mobile navigation.
45. Changes the project action to “Continue brief” for partial drafts.
46. Changes the project action to “Continue project” for completed drafts.
47. Keeps “Start a project” when no local draft exists.
48. Makes mobile navigation vertically scrollable on short screens.
49. Contains overscroll within the open navigation sheet.
50. Accounts for bottom safe-area inset inside the menu.

## Keyboard, touch and assistive UX

51. Standardizes a high-visibility gold focus indicator.
52. Preserves extra focus contrast for primary CTAs.
53. Applies focus visibility to links, buttons and form fields.
54. Applies focus visibility to disclosure summaries.
55. Uses system Highlight color in forced-colors mode.
56. Preserves visible borders for the mobile CTA in forced colors.
57. Sets at least 44px targets for coarse-pointer buttons.
58. Sets at least 44px targets for coarse-pointer form fields.
59. Sets at least 44px targets for disclosure summaries.
60. Sets at least 44px targets for primary and secondary CTAs.
61. Increases checkbox and radio targets for touch input.
62. Gives mobile navigation links 48px minimum height.
63. Adds touch-action manipulation within workspaces.
64. Adds a restrained tap-highlight color to interactive elements.
65. Shows a not-allowed cursor for unavailable controls.
66. Shows a progress cursor for busy regions.
67. Retains semantic button elements for all navigator actions.
68. Exposes navigator expansion state with aria-expanded.
69. Connects the navigator trigger and panel with aria-controls.
70. Hides the decorative progress bar from assistive technology.

## Readability and layout resilience

71. Prevents browser text-size adjustment from destabilizing layouts.
72. Enables optimized legibility for body text.
73. Prevents long prose from overflowing cards.
74. Prevents long labels from overflowing form controls.
75. Constrains form controls to their containers.
76. Uses a legible placeholder color instead of reduced opacity.
77. Applies safe scroll padding beneath stacked sticky navigation.
78. Applies safe anchor margins to every workspace tool.
79. Uses mobile-specific anchor margins for the shorter header.
80. Clips accidental horizontal overflow within mobile workspaces.
81. Makes the directory a four-column scan on wide screens.
82. Reduces the directory to two columns on tablets.
83. Reduces the directory to one column on small phones.
84. Keeps current tool names on one readable truncated line.
85. Preserves a compact current-tool row on narrow phones.
86. Stacks directory search and workspace-top action on phones.
87. Keeps directory search at a 16px mobile-safe text size.
88. Separates navigation controls with visible borders.
89. Highlights the current tool with color and an inset marker.
90. Provides hover-independent selected state through aria-current.

## Performance, motion and alternate output

91. Uses passive scroll tracking for workspace progress.
92. Removes scroll and keyboard listeners when the navigator unmounts.
93. Disconnects IntersectionObserver when the navigator unmounts.
94. Reduces rendering work for off-screen, long workspace modules.
95. Provides intrinsic placeholder sizing to limit scroll jumps.
96. Disables smooth scrolling when reduced motion is requested.
97. Collapses transitions and animation loops under reduced motion.
98. Expands deferred workspace content when printing.
99. Hides navigation, install prompts and transient notices when printing.
100. Prints external link destinations so exported project records retain context.
