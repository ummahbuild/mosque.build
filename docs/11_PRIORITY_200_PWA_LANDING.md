# Priority 200 PWA + Landing Batch

The second delivery pass adds `/features/priority-200`, covering distinct canonical records 101–200 from the same deterministic candidate ordering documented for the first batch.

## Additional executable behavior

- priority, title and implementation-wave sorting
- comfortable and compact density modes
- cumulative canonical numbering across both batches
- filter state encoded in the URL for shareable review views
- a single private device shortlist shared across both batches
- a reusable server-side selection module so batch slices cannot drift
- second-batch sitemap, validation and offline-shell coverage

The source registry contains 756 eligible candidates as of this pass. Batch inclusion remains a review/discovery milestone; it does not upgrade canonical feature status.
