import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";import {assetGaps,nextServiceDate,serviceState} from "../apps/web/lib/asset-lifecycle.ts";
test("maintenance schedule is deterministic",()=>{const asset={commissionedAt:"2026-01-01",lastServiceAt:"",intervalDays:90};assert.equal(nextServiceDate(asset),"2026-04-01");assert.equal(serviceState(asset,"2026-04-02"),"overdue");assert.equal(serviceState(asset,"2026-03-15"),"due-soon")});
test("last service replaces commissioning schedule basis",()=>assert.equal(nextServiceDate({commissionedAt:"2026-01-01",lastServiceAt:"2026-05-10",intervalDays:30}),"2026-06-09"));
test("asset register requires accountable minimum fields",()=>assert.deepEqual(assetGaps({name:"",category:"",locationRef:"",assetRef:"",responsibleRole:"",intervalDays:0}),["asset name","category","location reference","asset reference","responsible role","service interval"]));
test("operations UI preserves statutory boundary",()=>{const ui=fs.readFileSync(new URL("../apps/web/components/operations-workspace.tsx",import.meta.url),"utf8");assert.match(ui,/not credential verification or statutory approval/);assert.match(ui,/responsible role—not a person/i);assert.match(ui,/location reference—not an address/i)});
