import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("design studio keeps geometry renderer-neutral and review boundaries visible",async()=>{const source=await readFile(new URL("../apps/web/lib/mosque-massing.ts",import.meta.url),"utf8");assert.doesNotMatch(source,/THREE\.|Babylon|Filament/);const studio=await readFile(new URL("../apps/web/components/mosque-design-studio.tsx",import.meta.url),"utf8");assert.match(studio,/conceptual massing/);assert.match(studio,/not a measured drawing/);assert.match(studio,/Site \+ qibla verification/)});
test("design scenarios expose stable project ids",async()=>{const source=await readFile(new URL("../apps/web/lib/mosque-massing.ts",import.meta.url),"utf8");for(const id of ["compact","courtyard","community"])assert.match(source,new RegExp(`id:\"${id}\"`))});
