import assert from "node:assert/strict";import test from "node:test";import {readFile} from "node:fs/promises";const ui=await readFile(new URL("../apps/web/components/material-assembly-library.tsx",import.meta.url),"utf8");test("material library supports search comparison persistence and empty state",()=>{for(const token of ["type=\"search\"","Compare","localStorage","No matching assemblies","Selection boundary"])assert.match(ui,new RegExp(token));assert.match(ui,/length<3/)});
