import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";import {drillGaps,emergencyModes,planCoverage} from "../apps/web/lib/emergency-operations.ts";
test("emergency modes cover roadmap conditions",()=>assert.deepEqual(emergencyModes.map(item=>item.id),["heat","flood","smoke","storm","water","compound"]));
test("plan coverage is deterministic and evidence-driven",()=>{const blank={leadRole:"",warningSource:"",accessibleChannel:"",backupChannel:"",checks:[],notes:"",reviewedAt:""};assert.equal(planCoverage(blank,6),0);assert.equal(planCoverage({...blank,leadRole:"Duty manager",warningSource:"Authority alert",accessibleChannel:"Visual and audible",backupChannel:"Runner",checks:["one","two"]},6),60)});
test("drills require role scenario and observed outcome",()=>assert.deepEqual(drillGaps({date:"",facilitatorRole:"",scenario:"",outcome:""}),["drill date","facilitator role","scenario tested","observed outcome"]));
test("emergency UI preserves warning and privacy boundaries",()=>{const ui=fs.readFileSync(new URL("../apps/web/components/emergency-readiness-workspace.tsx",import.meta.url),"utf8");assert.match(ui,/does not issue alerts or predict hazards/);assert.match(ui,/avoid personal or medical details/);assert.match(ui,/Follow current official warnings and emergency services/)});
test("emergency UI validates restored plans and drill records",()=>{const ui=fs.readFileSync(new URL("../apps/web/components/emergency-readiness-workspace.tsx",import.meta.url),"utf8");assert.match(ui,/function safePlan/);assert.match(ui,/emergencyModes\.some/);assert.match(ui,/slice\(0,100\)/)});
