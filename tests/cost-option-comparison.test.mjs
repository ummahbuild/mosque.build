import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";
const domain=fs.readFileSync(new URL("../apps/web/lib/cost-option-comparison.ts",import.meta.url),"utf8"),ui=fs.readFileSync(new URL("../apps/web/components/cost-option-comparison-workbench.tsx",import.meta.url),"utf8"),css=fs.readFileSync(new URL("../apps/web/components/cost-option-comparison-workbench.css",import.meta.url),"utf8"),page=fs.readFileSync(new URL("../apps/web/app/construction/page.tsx",import.meta.url),"utf8"),pkg=fs.readFileSync(new URL("../apps/web/lib/project-package.ts",import.meta.url),"utf8"),workflow=fs.readFileSync(new URL("../apps/web/lib/workflow-integration.ts",import.meta.url),"utf8");
test("option comparison is bounded and blocks misleading arithmetic",()=>{for(const term of["mosque.build/cost-option-comparison@1","records.length>100","baselineLineIds","alternativeLineIds","Cost-plan basis date missing","Price-level date missing","currencies differ","no conversion is inferred","price source missing","deltaPercent"])assert.match(domain,new RegExp(term.replace(/[.*+?^${}()|[\]\\]/g,"\\$&"),"i"));assert.doesNotMatch(domain,/recommendedOption|approvedBudget|forecastSaving/)});
test("comparison UI exposes scope lifecycle evidence and missing states",()=>{for(const term of["BASELINE + ALTERNATIVE","Comparable scope reference","Model revision reference","Lifecycle, worship, accessibility and operations effects","Comparison unavailable or incomplete","Lower cost does not establish better value","aria-live"])assert.ok(ui.toLowerCase().includes(term.toLowerCase()),term);assert.match(css,/min-height:44px/);assert.match(css,/@media\(max-width:650px\)/)});
test("option studies join construction package and workflow",()=>{assert.match(page,/CostOptionComparisonWorkbench/);assert.match(pkg,/costOptionComparison/);assert.match(workflow,/costOptionComparison/)});
