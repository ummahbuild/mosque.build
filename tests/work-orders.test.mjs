import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";import {canComplete,workOrderDueState,workOrderGaps} from "../apps/web/lib/work-orders.ts";
test("work-order due states are deterministic",()=>{assert.equal(workOrderDueState({dueAt:"2026-09-01",status:"open"},"2026-09-08"),"overdue");assert.equal(workOrderDueState({dueAt:"2026-09-12",status:"open"},"2026-09-08"),"due-soon");assert.equal(workOrderDueState({dueAt:"",status:"open"},"2026-09-08"),"unscheduled");assert.equal(workOrderDueState({dueAt:"2020-01-01",status:"complete"},"2026-09-08"),"complete")});
test("completion requires an evidence reference",()=>{assert.equal(canComplete({completionEvidence:""}),false);assert.equal(canComplete({completionEvidence:" service report SR-12 "}),true)});
test("work orders require accountable minimum fields",()=>assert.deepEqual(workOrderGaps({title:"",category:"",reportedAt:"",responsibleRole:"",description:""}),["task title","category","reported date","responsible role","factual description"]));
test("work-order UI keeps closure and personal-data boundaries",()=>{const ui=fs.readFileSync(new URL("../apps/web/components/work-order-workspace.tsx",import.meta.url),"utf8");assert.match(ui,/not a completed inspection or instruction/);assert.match(ui,/responsible role—not a person/i);assert.match(ui,/Regulated systems require competent testing/)});
