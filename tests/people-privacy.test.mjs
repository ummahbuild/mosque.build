import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";import {accessRisk,retentionState,roleGaps} from "../apps/web/lib/people-privacy.ts";
test("role records require purpose ownership and retention",()=>assert.deepEqual(roleGaps({roleTitle:"",coverageArea:"",purpose:"",dataOwnerRole:"",retentionReviewAt:""}),["role title","coverage area","specific purpose","data owner role","retention review date"]));
test("retention state is deterministic",()=>{assert.equal(retentionState({retentionReviewAt:"2026-09-01"},"2026-09-08"),"overdue");assert.equal(retentionState({retentionReviewAt:"2026-09-20"},"2026-09-08"),"due-soon");assert.equal(retentionState({retentionReviewAt:"2027-01-01"},"2026-09-08"),"planned")});
test("sensitive access exposes missing governance review",()=>{assert.equal(accessRisk({accessScopes:["Financial records"],privacyBasisState:"not-reviewed",safeguardingState:"not-applicable"}),"review-needed");assert.equal(accessRisk({accessScopes:["Operations records"],privacyBasisState:"not-reviewed",safeguardingState:"not-applicable"}),"bounded")});
test("privacy UI excludes direct identifiers and preserves governance boundary",()=>{const ui=fs.readFileSync(new URL("../apps/web/components/people-privacy-workspace.tsx",import.meta.url),"utf8");assert.match(ui,/Do not enter here/);assert.match(ui,/not a personnel, volunteer, safeguarding, medical or background-check system/);assert.match(ui,/Role title—not a person/);assert.match(ui,/No personal roster was included/)});
