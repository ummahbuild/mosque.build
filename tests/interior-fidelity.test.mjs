import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),"utf8"),domain=read("apps/web/lib/interior-decoration.ts"),view=read("apps/web/components/interior-three-preview.tsx"),ledger=read("docs/76_INTERIOR_FIDELITY_100.md");
test("decoration records preserve material craft light and care evidence",()=>{for(const field of["ornamentMode","ornamentDensity","wallFinish","floorFinish","lightTemperatureK","glareReview","materialSource","craftAuthorRole","cleaningPlan","sampleReference"])assert.match(domain,new RegExp(field));assert.match(domain,/Math\.max\(2200,Math\.min\(6500/);assert.match(domain,/ornament authorship/)});
test("interior renderer exposes coordinated spatial layers and views",()=>{for(const value of["wall-finish-intent","qibla-focal-zone","prayer-row-guide","lighting-intent","furnishing-need","Prayer eye level"])assert.match(view,new RegExp(value));assert.match(view,/no generated calligraphy/);assert.match(view,/renderer\.dispose/)});
test("interior delivery ledger records exactly 100 improvements",()=>{const items=ledger.match(/^\d+\./gm)??[];assert.equal(items.length,100);assert.deepEqual(items.map(x=>Number(x)),Array.from({length:100},(_,i)=>i+1))});
