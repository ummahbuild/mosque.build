import test from"node:test";import assert from"node:assert/strict";import fs from"node:fs";import{canAdvanceMaterial,emptyMaterialSelection,materialGaps,materialSummary,MATERIAL_SELECTION_SCHEMA,safeMaterialSelection}from"../apps/web/lib/material-selection.ts";import{projectSections}from"../apps/web/lib/project-package.ts";
const now="2026-09-09T00:00:00.000Z",base={id:"m1",title:"Floor finish study",assemblyGroup:"Prayer floor",area:"Prayer hall",application:"Replaceable worship finish",stage:"candidate",sampleStage:"not-requested",decisionStatus:"open",responsibleRole:"Architect",exposures:["High footfall"],sourceRef:"Internal sample register",observedAt:"2026-09-09",rights:"Project record",performanceEvidence:"Evidence references pending review",sampleRef:"",mockupRef:"",substrate:"",interfaceNotes:"",storagePlan:"",installationMethodRef:"",inspectionRef:"",maintenancePlan:"",spareStrategy:"",endOfLifePlan:"",substitutionReason:"",substitutionEvidence:"",decisionRef:"",notes:"",createdAt:now,updatedAt:now};
test("material register is bounded portable and project-integrated",()=>{const s={...emptyMaterialSelection(),records:[base]};assert.equal(s.schema,MATERIAL_SELECTION_SCHEMA);assert.equal(safeMaterialSelection(s)?.records.length,1);assert.equal(safeMaterialSelection({...s,records:Array(201).fill(base)}),null);assert.equal(projectSections.find(x=>x.id==="materialSelection")?.key,"mosque-build.material-selection.v1")});
test("stage gates require evidence, mockups, installation and acceptance records",()=>{assert.equal(canAdvanceMaterial(base,"review"),false);assert.ok(materialGaps({...base,stage:"accepted"}).includes("inspection and acceptance record"));assert.deepEqual(materialSummary([base]),{total:1,evidenceCoverage:100,samples:0,accepted:0,holds:0,substitutions:0})});
test("UI keeps claims and professional boundaries explicit",()=>{const text=fs.readFileSync(new URL("../apps/web/components/material-selection-workbench.tsx",import.meta.url),"utf8");assert.match(text,/does not verify a manufacturer/);assert.match(text,/qualified review/);assert.doesNotMatch(text,/guaranteed|authority approved|certified product|in stock/i)});
