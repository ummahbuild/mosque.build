import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";
const read=path=>fs.readFileSync(new URL(`../${path}`,import.meta.url),"utf8");
test("model quality engine checks geometry and evidence boundaries",()=>{const engine=read("apps/web/lib/model-quality.ts");for(const id of ["schema","ids","bounds","dimensions","overlap","area","guides","verification","culture","professional"])assert.match(engine,new RegExp(`\\"${id}\\"`));assert.match(engine,/professionally verified; credentials are not verified/);assert.match(engine,/precedent study, not authenticity approval/)});
test("model quality report is filterable accessible and portable",()=>{const panel=read("apps/web/components/model-quality-panel.tsx");assert.match(panel,/mosque\.build\/model-quality-report@1/);assert.match(panel,/aria-pressed/);assert.match(panel,/No checks match this filter/);assert.match(panel,/Geometry consistency only/);assert.match(panel,/mosque-build:schematic-change/)});
test("shared room boundaries use a floating point tolerance",()=>{const engine=read("apps/web/lib/model-quality.ts");assert.match(engine,/const tolerance=\.001/);assert.match(engine,/b\.width-tolerance/);assert.match(engine,/b\.depth-tolerance/)});
