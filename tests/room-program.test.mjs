import test from "node:test";import assert from "node:assert/strict";import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),"utf8"),domain=read("apps/web/lib/room-program.ts"),ui=read("apps/web/components/room-program-workbench.tsx"),pkg=read("apps/web/lib/project-package.ts");
test("room program is versioned renderer-neutral and bounded",()=>{assert.match(domain,/mosque\.build\/room-program@1/);assert.match(domain,/requirements\.length<=100/);assert.match(domain,/alternatives\.length<=12/);assert.doesNotMatch(domain,/three|Mesh|WebGL/i)});test("room requirements cover architecture intent and review",()=>{for(const value of ["purpose","priority","accessibleJourney","privacy","acoustic","daylight","adjacentTo","separateFrom","reviewState"])assert.match(domain,new RegExp(value));for(const value of ["VERSIONED ROOM PROGRAM","Should be adjacent","Should be separated","Save current alternative","code checks or approvals"])assert.match(ui,new RegExp(value,"i"))});test("room program travels in project package",()=>{assert.match(pkg,/roomProgram/);assert.match(pkg,/mosque-build\.room-program\.v1/)})
